build/
install/
log/
*.pcd
*.o
__pycache__/

# client private scan renders (do not publish)
map_topdown.png
map_perspective.png
map_perspective.jpg
all_points.pcd
*.pcd

# client-supplied reference material + private survey location (do not publish)
*.JPG
sync cable *.jpg
*.geo.txt

# diagnostics collected from a client machine — may contain credentials
mapper_diag.txt
mapper_build.log

# client screenshots pasted into chat (dashboard, pinouts) — not ours to publish
image.png
image.jpg

# renders made from the client's scans - same privacy as the .pcd itself
scan_overview*.png
livox_map_*.png
dash_check.png
